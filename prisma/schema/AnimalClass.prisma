model AnimalClass {
  id Int @id @default(autoincrement())
  name        String
  breeds      String[]
  availableAt BranchesToAnimalClasses[]
  contents    Content[]

  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  registeredPets Pet[]

  @@map("animal_classes")
}
