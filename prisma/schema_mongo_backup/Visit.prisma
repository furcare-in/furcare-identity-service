model Visit {
  id String @id @default(auto()) @map("_id") @db.ObjectId

  date      DateTime
  reason    String
  notes     String?
  status    String   @default("Scheduled") // Scheduled, In Progress, Completed, Cancelled

  clientId  String   @db.ObjectId
  client    Client   @relation(fields: [clientId], references: [id])
  
  petId     String   @db.ObjectId
  pet       Pet      @relation(fields: [petId], references: [id])
  
  moduleId String? @db.ObjectId // Optional reference to a medical record or other module

  // Medical Record Details
  author            String?
  markdown          String?
  selectedOptions   Json?
  vaccination       Json?
  prescriptions     Json?
  assessment        Json?
  vitals            Json?
  documents         Json?
  diagnostics       Json?

  // Payment Details
  paymentStatus     String   @default("Pending") // Pending, Completed, Failed
  paymentMethod     String?
  totalAmount       Float?
  oneTimeDiscount   Float?
  invoiceNumber     String?
  paymentDate       DateTime?

  businessBranchId String         @db.ObjectId
  businessBranch   BusinessBranch @relation(fields: [businessBranchId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("visits")
}
